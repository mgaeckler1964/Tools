<?php
//	$title = "Galerie";					// the title of the gallery (default is the directory path)
//	$treeTitle = "Galerie";				// the title for the image tree
//	$indent = 16;						// number of pixel to indent per tree level
//	$previewsPerLine = 2;				// number of gallery preview images per table row
										// when this value is set to 0, a float layout will be generated
//	$imagesPerLine = 2;					// number of images per table row
										// when this value is set to 0, a float layout will be generated

//	$iePreviewsPerLine = 2;				// these values are used, if an old browser which cannot display
//	$ieImagesPerLine = 2;				// the float layout correctly

//	$imagePosition = 0;					// where to show the image:
										// 0: In a popup area
										// 1: left to the left comment
										// 2: right to the left comment but left to the thumbnails
										// 3: left to the right comment but right to the thumbnails
										// 4: right to the right comment

//	$displayComment = true;				// display the title of a preview image
//	$displayDescription = true;			// display the description of a preview image
//	$displayCopyrightInfo = true;		// display the copyright notes of a preview image
//	$displayImageData = true;			// display date and size of images
//	$displayPreviewFilename = true;		// display the filename of a preview image
//	$displayArtist = true;				// display the fotographer of a preview image
//	$displayCameraSettings = true;		// display the settings of the camera

/*
	the following options are used for displaying single image directories
	without preview and large subdirectories
*/
//	$thumbWidth = 0;					// create thumbs for actual directory 
										// if 0 do not create thumbs otherwise set
										// to the width of the thumbs for landscape images
										// portraet images will get the height set here.
//	$thumbCache = 0;					// if not zero create a thumb cache, requires a write access 
										// for the web server
										
/*
	the following variables are used for the java script function
*/

//	$destMargin = 50;					// this is the margin for the full size picture area
//	$smalMargin = 512;					// this is the margin used before hiding the picture area
//	$growSpeed  = 10;					// this is the interval in ms to grow or shrink the picture area
//	$growIncrement = 10;				// this is the increment in pixel to grow or shrink the picture area
//	$imageWidth = 0;					// these two options are used to scale the image
//	$imageHeight = 0;					// by the browser. If the value is 0 no scaling happens
										// please note: if you set both values scaling will not
										// change aspect ratio the resulting image size will not be larger
										// than the values provides here.
										// if you set the values to "Auto" the width and/or height will
										// scaled to the size of the image area for best results set both
										// values to "Auto"

/*
	the following icons can be used for the image navigator
	by default, the java script does not use any icons
*/
//	$imagePrev = "imagePrev.gif";
//	$imageNext = "imageNext.gif";

//	$imageSmaller = "imageSmaller.gif";
//	$imageScreen = "imageScreen.gif";
//	$image100 = "image100.gif";
//	$imageBigger = "imageBigger.gif";

//	$imageClose = "imageClose.gif";
?>
